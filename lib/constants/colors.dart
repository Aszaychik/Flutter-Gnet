import 'package:flutter/material.dart';

class AppColors {
  // Primary blues (trustworthy and professional)
  static const Color primary = Color(0xFF2A4B8D);    // Deep navy blue
  static const Color primaryVariant = Color(0xFF1A365D);  // Darker navy

  // Accent teals (modern and energetic)
  static const Color secondary = Color(0xFF2CD5C4);    // Bright teal
  static const Color secondaryVariant = Color(0xFF1AA697);  // Saturated teal

  // Neutrals (clean and minimal)
  static const Color background = Color(0xFFF8FAFC);   // Very light cool gray
  static const Color surface = Color(0xFFFFFFFF);      // Pure white
  static const Color error = Color(0xFFE53935);        // Vibrant red for errors

  // Text/icon colors
  static const Color onPrimary = Color(0xFFFFFFFF);   // White on blues
  static const Color onSecondary = Color(0xFF000000);  // Black on teals
  static const Color onBackground = Color(0xFF2D3748); // Dark gray on backgrounds
  static const Color onSurface = Color(0xFF2D3748);    // Dark gray on surfaces
  static const Color onError = Color(0xFFFFFFFF);      // White on errors
}